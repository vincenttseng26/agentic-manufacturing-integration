# 複製成 .env 並填入真實值（.env 已被 .gitignore 排除，不會 commit）
# cp .env.example .env

# --- Gemini Agent (Phase 3) ---
GEMINI_API_KEY=your-gemini-api-key-here

# --- PostgreSQL（預設對齊 docker-compose.yml）---
PGHOST=localhost
PGPORT=5432
PGDATABASE=mfg
PGUSER=postgres
PGPASSWORD=devpassword
