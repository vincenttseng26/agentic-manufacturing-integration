[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
# Isaac Lab scripts must create the AppLauncher before importing torch / gym / isaaclab,
# so module-level imports intentionally appear after that call (E402).
"sim/*.py" = ["E402"]
